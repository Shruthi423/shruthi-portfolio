import HomeV2 from "@/app/components/home/HomeV2";

// The home is the one-pager: hero footprints, work grid, about, footer — all on
// a single freely-scrolling page. Rendered bare by SiteFrame.
export default function HomePage() {
  return <HomeV2 />;
}
